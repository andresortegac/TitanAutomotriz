<IfModule mod_rewrite.c>
    RewriteEngine On

    # The hosting document root is the repository root; Laravel's front
    # controller and static assets live in public/.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
